/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com','encrypted-tbn2.gstatic.com','www.converse.in'],
  },
  // ... your other config options
}

export default nextConfig;
